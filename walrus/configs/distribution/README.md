## Key settings overview

Mostly self-explanatory:

```yaml
distribution_type: local # Distribution types "local", "ddp", "fsdp", "hsdp"
local_size: # For HSDP, if sub-dividing nodes, specify to what degree here.
```