defaults:
- field_index_map_override: bc_only_override
well_base_path: /mnt/gpuxl/polymathic/the_well/datasets/
wandb_data_name: rayleigh_taylor_instability
module_parameters:
  _target_: walrus.data.MixedWellDataModule
  batch_size: 8
  n_steps_input: 10
  n_steps_output: 1
  min_dt_stride: 1
  max_dt_stride: 1
  max_samples: 2000
  well_dataset_info:
    rayleigh_taylor_instability:
      include_filters: []
      exclude_filters: []
